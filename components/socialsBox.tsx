const SocialsBox = () => {
    return (
        <div className='fixed bottom-32 rounded-full bg-slate-400 w-[clamp(150px,10vw,350px)] h-12 ml-[clamp(1%,4vw,12%)]'>
            
        </div>
    );
};

export default SocialsBox;
